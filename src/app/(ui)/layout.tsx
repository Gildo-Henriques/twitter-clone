import type { Metadata } from "next";
import LogoX from "../../components/ui/LogoX";
import Navbar from "../../components/ui/Navbar";
import Button from "../../components/ui/Button";
export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`antialiased bg-black`}>
        <main className="flex justify-between w-full">
          <section className="sticky flex-1 border-right py-8 h-screen top-0 left-0 hidden lg:flex items-center lg:flex-col">
            <div className="flex flex-col justify-between h-screen">
            <div className="flex justify-start">
            <LogoX size={35} position={1} />
            </div>
            <Navbar />
           <div className="">
            <Button size={1}>
                Hello
            </Button>
           </div>
            </div>
          </section>
          {children}
          <aside className="sticky h-screen flex-1 border-left top-0 right-0 hidden lg:flex lg:flex-col">
            <h1 className="text-white">direita</h1>
          </aside>
        </main>
      </body>
    </html>
  );
}
